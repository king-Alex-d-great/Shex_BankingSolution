@using WebUI.domain.Controllers;
@model List<AppRole>

@{
    ViewData["Title"] = "Index";
}

<h1>Role Manager</h1>

<form method="post" asp-action="AddRole" asp-controller="Role">
    <div class="input-group">
        <input name="roleName" class="form-control w-25">
        <span class="input-group-btn">
            <button class="btn btn-info">Add New Role</button>
        </span>
    </div>
</form>

@*@foreach (var role in Model)
	{
<div class="card mb-3">
    <div class="card-header">
        Id : @role.Id
    </div>
    <div class="card-body">
        <h5 class="card-title">Role: @role.Name</h5>
    </div>
    <div class="card-body">
        <h5 class="card-title">Date Created: @role.CreatedAt</h5>
    </div>
    <div class="card-body">
        <h5 class="card-title">Created By: @role.CreatedBy</h5>
    </div>
    <div class="card-footer">
        <form asp-action="DeleteRole" asp-controller="Role" method="post">
            <a asp-controller="Role" asp-action="UpdateRole" asp-route-id="@role.Id" class="btn btn-info">Update</a>
            <button type="submit" class="btn btn-danger" onclick="return confirm('Are you sure you want to delete role : @role.Name')">Delete</button>
        </form>
    </div>
</div>
		 
	}*@


<table class="table table-striped">
    <thead>
        <tr>
            <th>Id</th>
            <th>Role</th>
            <th>Date Created</th>
            <th>Creator</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var role in Model)
        {
        <tr>
            <td>@role.Id</td>
            <td>@role.Name</td>
            <td>@role.CreatedAt</td>
            <td>@role.CreatedBy</td>
                <td><a asp-controller="Role" asp-action="UpdateRole" asp-route-id="@role.Id" class="btn btn-info">Update</a></td>
                <td><a asp-controller="Role" asp-action="DeleteRole" class="btn btn-info" asp-route-id="@role.Name">Delete</a></td>
        </tr>
        }
    </tbody>
</table>



